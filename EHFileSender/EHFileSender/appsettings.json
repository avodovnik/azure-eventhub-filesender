{
  "eventHubConnStr": "", /* EH Connection string. Needs send rights, nothing else.*/
  "detailedReporting": true, /* Output more info like task counts and statuses */
  "testForJson": true, /* Try to deserialise a line from JSON to see if it's valid */
  "giveUpAfterNonJson": false, /* Give up if the line can't be deserialised to JSON, or just skip the line? */
  "degreeOfParallelism": 32, /* How many tasks to use: 32 is decent balance between throughput and being able to keep track of what's going on */
  "simulateSending": true /* don't actually bother sending to the event hub, just batch up and wait 10 seconds per thread. Useful for testing. */
}